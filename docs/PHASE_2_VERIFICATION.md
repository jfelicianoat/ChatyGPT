# Evidencias de Fase 2

Fecha: 2026-07-26.

Estado: **Fase 2 en curso**. Este corte cubre memoria visible, manual, opt-in,
el probador de recuperación semántica y la selección semántica opcional dentro
del envío del chat, además del primer corte de resúmenes controlados.

## Matriz del corte

| Requisito | Estado | Evidencia | Resultado | Manual pendiente |
|---|---|---|---|---|
| Memoria desactivada por defecto | Verificado automáticamente | `feature_flags.memory = 0` y consulta defensiva en Rust | ningún recuerdo entra en un turno hasta activar la función | comprobar estado inicial en Inicio |
| Creación manual y visible | Verificado por tipos y compilación | formulario de Memoria + validación Rust de categoría, sensibilidad y 2.000 caracteres | solo el usuario crea recuerdos | crear un recuerdo desde la ventana |
| Edición completa de recuerdos | Verificado automáticamente | comando `update_memory_item`, editor inline y test `editing_memory_preserves_or_invalidates_its_index_by_content` | contenido, categoría, sensibilidad y ámbito pueden corregirse; los cambios de metadatos conservan el índice | Inicio → Memoria → Editar, guardar primero solo un cambio de ámbito |
| Reindexación segura al corregir texto | Verificado automáticamente | el contenido modificado elimina el embedding anterior, crea una tarea nueva y el test `stale_embedding_result_cannot_replace_an_edited_memory_index` rechaza resultados tardíos | una versión antigua nunca vuelve a figurar como `Índice preparado` después de editar | editar el texto y observar `Indexando…` hasta `Índice preparado` |
| Ámbito global o proyecto | Verificado automáticamente | test Rust `memory_is_opt_in_scoped_and_user_controllable` | un recuerdo de proyecto no aparece en chats ajenos | comparar dos conversaciones |
| Control individual | Verificado automáticamente | activar, desactivar y borrar con confirmación | los elementos desactivados quedan fuera del contexto | probar los tres estados |
| Inclusión en el turno | Verificado automáticamente | test `approved_memory_is_visible_in_request_and_absent_without_items` | el prompt contiene únicamente recuerdos aprobados | preguntar por una preferencia guardada |
| Trazabilidad | Verificado automáticamente | snapshot `window-memory-v1` y fuente `memory` por recuerdo | se conserva qué memoria se utilizó en cada tarea | inspector detallado de contexto futuro |
| Inspector de contexto | Verificado automáticamente | comando `get_task_context` y proyección sin identificadores internos ni JSON técnico | cada respuesta permite revisar estrategia, tokens estimados, mensajes y recuerdos con su motivo | abrir `Ver contexto utilizado` bajo una respuesta |
| Auditoría | Verificado por repositorio y compilación | eventos `memory.enabled`, `created`, `item_enabled`, `item_disabled`, `deleted` | operaciones visibles en Actividad reciente sin mostrar el contenido | revisar cronología en Inicio |
| Indexación local | Verificado automáticamente | tarea durable `inference_kind=embedding`, `risk.data_classification=local_only` y coste máximo cero | test `memory_embedding_request_is_local_only_and_traceable` | guardar un recuerdo con Broker conectado |
| Contrato Broker 2.7 | Verificado automáticamente | mantiene la compatibilidad de ingesta 2.6 y reconoce `waiting_for_memory` como espera no terminal | tests contractuales 2.6/2.7 y comprobación de OpenAPI | abrir Inicio y comprobar `Contrato 2.7` |
| Descubrimiento al arrancar | Verificado por tipos y compilación | `GET /api/v1/capabilities` se ejecuta una vez durante el arranque y se presenta en Inicio | carriles, frontera derivada, sandbox y map-reduce quedan visibles sin lanzar una tarea | reiniciar con Broker conectado |
| Esquema estricto 2.7 | Verificado automáticamente | las peticiones usan `priority`, omiten campos ajenos y dejan que el Broker derive la frontera desde `risk` | evita HTTP 422 y aprovecha la cola priorizada | enviar un chat normal y otro con memoria sensible |
| Preferencias por conversación | Verificado automáticamente | migración 0009 y test `conversation_execution_preferences_are_validated_persisted_and_visible` | privacidad, estrategia, preset, presupuesto y documentos largos sobreviven al reinicio sin afectar a otros chats | cambiar opciones, cerrar y reabrir la conversación |
| Frontera local/cloud | Verificado automáticamente | `risk.data_classification` se envía sin `cloud_allowed` ni `allowed_providers`; los recuerdos sensibles fuerzan `local_only` | `internal/public` permiten proveedores cloud y `confidential/local_only` los bloquean según contrato | enviar con Uso personal y revisar el modelo; repetir con Solo en este equipo |
| Estrategias y presets descubiertos | Verificado automáticamente | la interfaz filtra `auto`, `mixture_of_agents` y profundidad con `capabilities.strategies/presets` | no ofrece combinaciones que el Broker declara incompatibles | abrir Opciones de ejecución con Broker conectado |
| Presupuesto duro | Verificado automáticamente | `max_cost_usd` durable, validado entre 0 y 10 USD y aplicado también a resúmenes | cada conversación limita el gasto de proveedores de pago | elegir 0 USD y 0,10 USD y comparar elegibilidad |
| Documentos largos explícitos | Verificado automáticamente | test `conversation_preferences_enable_auto_routing_budget_and_long_documents` | map-reduce solo se envía con adjunto completo y estrategia single/auto; nunca se trunca en silencio | adjuntar un PDF grande y activar Dividir documentos que no caben |
| Análisis colaborativo | Verificado automáticamente | test `collaborative_analysis_uses_the_selected_depth_without_invalid_map_reduce` | fast/slow se traducen a mixture_of_agents con 3 proponentes y planificación adaptativa | elegir Análisis en equipo y revisar el modelo usado |
| Prioridad por conversación | Verificado automáticamente | la preferencia se valida, persiste y se envía como `priority` 0–1000 | permite Alta, Normal o Baja sin exponer números técnicos | abrir Opciones de ejecución y cambiar Prioridad en la cola |
| CSV/TSV/Excel con sandbox | Verificado automáticamente | el cliente exige consentimiento, conserva el `broker_file` y añade `run_code` | evita `TABULAR_ATTACHMENT_REQUIRES_SANDBOX` y permite cálculos reales | adjuntar un CSV y enviar una pregunta; aceptar “Permitir y enviar” |
| Código en análisis colaborativo | Verificado automáticamente | `mixture_of_agents` recibe `proposer_skills: ["run_code"]` | mantiene el análisis en equipo en vez de degradarlo a agente | seleccionar Análisis en equipo, adjuntar CSV y autorizar código |
| Progreso estructurado | Verificado automáticamente | test `broker_progress_is_persisted_for_the_visible_task_snapshot` y presentación `taskProgressSummary` | fase y 2/3 invocaciones se conservan y muestran sin exponer estados técnicos | ejecutar un análisis en equipo y observar la barra |
| Error recuperable legible | Verificado automáticamente | `taskFailureSummary` interpreta `{code,message,retryable}` | la respuesta explica el fallo y si conviene repetir, sin mostrar JSON | provocar un proveedor no disponible o detenerlo durante una tarea |
| Renombrado solo bajo petición | Verificado automáticamente | tests `tools_mode_does_not_offer_rename_for_an_unrelated_request` y `tools_mode_uses_agent_passthrough_only_when_enabled` | una petición normal no ofrece al modelo la acción de renombrar ni provoca autorizaciones repetidas | activar Herramientas, enviar varios mensajes normales y después pedir `Renombra el chat` |
| Alcance actual de adjuntos | Verificado automáticamente | tests `current_attachment_scope_overrides_removed_books_mentioned_in_history` y `attachmentSelectionOnConversationOpen` | los libros retirados permanecen en el historial visible, pero dejan de ser candidatos para referencias ambiguas; los que siguen vinculados se activan al abrir el chat | cargar dos libros, retirar uno, reabrir el chat y preguntar `¿Cuántos temas tiene?` |
| Seguimiento inteligente del chat | Verificado automáticamente | tests de `shouldFollowConversationScroll` y contenedor `.message-list` enlazado al ciclo de sondeo | una petición nueva desplaza hasta su respuesta; subir manualmente suspende el seguimiento hasta regresar cerca del final o enviar otro mensaje | enviar una petición larga, observar la respuesta y subir durante la espera |
| Tiempo visible de respuesta | Verificado automáticamente | `broker_tasks.terminal_at`, proyección `responseDurationMs` y `formatResponseDuration` | cada respuesta terminada muestra el tiempo total y lo conserva al reabrir el chat | enviar una petición y comprobar el dato junto al modelo utilizado |
| Persistencia vectorial | Verificado automáticamente | vector binario `f64`, dimensiones, modelo y SHA-256 en `embedding_records` | test `completed_memory_embedding_is_stored_with_model_and_dimensions` | comprobar estado `Índice preparado` |
| Estado y reintento | Verificado por tipos y compilación | estados `missing/indexing/ready/failed`, sondeo visual y botón `Indexar` | un fallo no elimina ni desactiva el recuerdo | detener Broker, crear y reintentar después |
| Contrato de embedding | Verificado automáticamente contra el esquema real del Broker | `selection_mode` no se envía dentro de `model_requirements`; la selección automática usa `execution.selection` por defecto | regresión `memory_embedding_request_is_local_only_and_traceable` | reintentar un recuerdo rechazado por la versión anterior |
| Error visible | Verificado automáticamente | tareas `orphaned` con `error_json` se presentan como `Error de índice` y muestran un mensaje limitado a 500 caracteres | el HTTP 422 deja de parecer un índice ausente | comprobar el recuerdo que falló originalmente |
| Recuperación tras reinicio | Revisado y compilado | la indexación usa `broker_tasks` y el mismo recuperador durable que el chat | la tarea continúa y el aviso la identifica como indexación de memoria | cerrar durante `Indexando…` |
| Búsqueda semántica manual | Verificado automáticamente | tarea durable para vectorizar la consulta y cálculo coseno local | test `semantic_memory_search_ranks_compatible_vectors_and_respects_scope` | escribir una consulta en Inicio → Memoria |
| Compatibilidad del índice | Verificado automáticamente | solo se comparan recuerdos con el mismo modelo y dimensiones que la consulta | evita mezclar espacios vectoriales incompatibles | indexar recuerdos con modelos distintos |
| Resultado explicable | Verificado por tipos y compilación | porcentaje, nivel de coincidencia, ámbito y texto original visibles | el usuario puede auditar por qué apareció cada recuerdo | ejecutar una búsqueda con dos recuerdos relacionados |
| Consulta recuperable | Revisado y compilado | tabla `memory_searches`, tarea asociada y reapertura de la última búsqueda | una búsqueda pendiente continúa tras reiniciar la app | cerrar mientras muestra `Buscando…` |
| Selección semántica por turno | Verificado automáticamente | control `Buscar recuerdos` y test `completed_semantic_search_prepares_chat_with_ranked_memory_and_trace` | solo se incluyen hasta cinco recuerdos compatibles y relacionados | activar el control y preguntar por una preferencia indexada |
| Persistencia antes de vectorizar | Verificado automáticamente | test `semantic_chat_persists_the_turn_before_requesting_its_query_embedding` y tabla `semantic_chat_workflows` | usuario, respuesta pendiente y tarea de búsqueda quedan en una sola transacción | cerrar la app mientras indica `Buscando recuerdos relacionados` |
| Recuperación de las dos etapas | Verificado automáticamente | consulta `semantic_chat_workflows_ready_to_continue` y recuperador de arranque | una búsqueda ya completada crea la tarea final incluso tras reiniciar | reiniciar entre la búsqueda y la generación |
| Trazabilidad semántica | Verificado automáticamente | snapshot `window-semantic-memory-v1`, puntuación y motivo en `context_sources` | `Ver contexto utilizado` explica qué recuerdos entraron y por qué | abrir el inspector bajo la respuesta |
| Privacidad de recuerdos sensibles | Verificado automáticamente | la tarea final reutiliza el enrutamiento de memoria sensible | un recuerdo sensible limita la generación a Ollama/LM Studio | buscar un recuerdo sensible y comprobar el proveedor |
| Generación de resumen durable | Verificado automáticamente | tabla `conversation_summaries`, tarea Broker asociada y test `generated_conversation_summary_is_an_inactive_draft` | el resultado remoto queda como borrador y nunca se activa solo | pulsar `Resumen` → `Generar borrador` |
| Edición y aprobación explícitas | Verificado automáticamente | test `approved_edited_summary_compacts_context_without_deleting_messages` | solo el texto guardado y aprobado pasa a estar activo | editar el borrador y pulsar `Guardar y aprobar` |
| Historial preservado | Verificado automáticamente | el resumen referencia `source_through_sequence`; los mensajes originales permanecen en `messages` | compactar contexto no elimina ni reescribe el chat | cerrar el panel y revisar mensajes anteriores |
| Trazabilidad del resumen | Verificado automáticamente | estrategia `window-summary-v1` y fuente `summary` con razón legible | `Ver contexto utilizado` identifica el resumen aprobado | enviar otro mensaje y abrir su inspector |
| Recuperación tras reinicio | Revisado y compilado | la generación reutiliza `broker_tasks`, idempotencia y recuperador de inicio | un borrador en generación continúa después de cerrar la app | cerrar mientras indica `Preparando borrador…` |
| Proveedores locales y cloud | Revisado por contrato | la generación permite Ollama, LM Studio, NVIDIA y DeepSeek con fallback | el Broker puede escoger entre proveedores habilitados | revisar el modelo usado por la tarea en Broker |
| Lote seguro de resumen | Verificado automáticamente | test `conversation_summary_input_is_bounded_and_leaves_newer_messages_uncovered` y presupuesto de 48.000 caracteres | el prompt de resumen no crece con todo el historial y deja los mensajes posteriores fuera | abrir un chat largo y comprobar la cobertura propuesta |
| Continuidad incremental | Verificado automáticamente | test `next_summary_input_merges_the_approved_summary_with_only_new_messages` | cada ronda consolida el resumen aprobado con mensajes nuevos, sin reenviar los ya cubiertos | aprobar un lote y pulsar después `Actualizar borrador` |
| Cobertura visible | Verificado por tipos y compilación | el panel expone total, cubiertos, restantes y cobertura del borrador | el usuario sabe exactamente qué sustituirá al aprobar | revisar los contadores antes y después de aprobar |
| Fragmentación estructural de adjuntos | Verificado automáticamente | tests `converted_markdown_is_split_into_bounded_chunks_without_losing_content` y `converted_markdown_prefers_document_boundaries` | bloques de hasta 4.000 caracteres que priorizan finales de párrafo o frase sin perder contenido | volver a preparar un PDF con secciones largas |
| Selección limitada y contextual | Verificado automáticamente | tests `document_chunk_selection_is_relevant_bounded_and_traceable` y `document_selection_includes_nearby_context_after_relevant_chunks` | como máximo 8 fragmentos y 24.000 caracteres; las coincidencias se completan con contexto vecino sin saltarse el presupuesto | preguntar por una sección cuyo título esté en el fragmento anterior |
| Cola semántica progresiva | Verificado automáticamente | test `document_embedding_queue_is_sequential_and_skips_failed_chunks` | una sola tarea por documento, continuación tras cada terminal y fallo aislado sin bloquear otros fragmentos | adjuntar un PDF largo y observar el contador |
| Embeddings documentales privados | Verificado automáticamente | test `document_chunk_embedding_request_is_local_only_and_traceable` | coste máximo cero, clasificación `local_only`, fuente y SHA-256 trazables | revisar una tarea de índice en Broker |
| Recuperación híbrida | Verificado automáticamente | test `hybrid_document_selection_uses_compatible_chunk_embeddings` | combina similitud coseno y coincidencia léxica solo con modelo y dimensiones compatibles | preguntar con sinónimos que no aparezcan literalmente |
| Fallback léxico | Verificado automáticamente | la selección híbrida acepta ausencia o incompatibilidad de vectores y `attachmentContextSummary` conserva el estado utilizable | un fallo semántico no impide consultar el documento | detener el motor de embeddings y repetir una pregunta literal |
| Sin duplicar documentos | Verificado automáticamente | test `selected_document_fragments_replace_the_full_broker_attachment` | cuando un documento tiene fragmentos locales no se envía además completo; los tabulares son la excepción contractual | comprobar una consulta sobre un PDF indexado y otra sobre un CSV |
| Trazabilidad documental | Verificado automáticamente | fuente `attachment_chunk`, ordinal, puntuación, motivo y extracto en `context_sources` | **Ver contexto utilizado** muestra el archivo y fragmento exactos | abrir el inspector bajo la respuesta |
| Acceso a la fuente original | Verificado automáticamente | referencia opaca en `ContextSourceView`, comando `reveal_context_source` y prueba `context_source_path_must_exist_inside_managed_storage` | **Mostrar archivo** selecciona la copia administrada en Windows sin devolver rutas ni ejecutar el adjunto | abrir una respuesta con fragmentos → Ver contexto utilizado → Mostrar archivo |
| Fuente local ausente | Verificado automáticamente | `sourceAvailable` se deriva al cargar el snapshot y `canRevealContextSource` cubre los estados de interfaz | el extracto histórico sigue visible, pero el botón queda deshabilitado con explicación | retirar temporalmente una copia administrada y reabrir el inspector |
| Recuperación de adjuntos anteriores | Revisado y compilado | el arranque vuelve a consultar adjuntos `ready` que aún no tienen fragmentos | documentos cargados antes de la migración se preparan sin volver a adjuntarlos | reiniciar y consultar el PDF existente |
| Errores de conversión accionables | Verificado automáticamente | `attachmentFailureGuidance` reconoce el límite `max_num_pages` y conserva las cifras del Broker | la tarjeta explica qué falló y cómo recuperarse sin obligar a interpretar JSON | abrir la conversación de `math-deep.pdf` |
| Re-adjunto tras cambiar la ingesta | Verificado automáticamente | test `reattaching_a_failed_file_starts_a_fresh_broker_conversion` | volver a adjuntar el mismo SHA-256 fallido descarta el `file_id` terminal y solicita una conversión nueva con la configuración actual del Broker | reiniciar la ingesta del Broker, volver a adjuntar el PDF y comprobar que aparece un `file_id` nuevo |
| Estados de adjunto legibles | Verificado automáticamente | `attachmentStatusLabel` traduce los seis estados de ingesta | la interfaz muestra `Convirtiendo`, `Preparado` o `No preparado` en lugar de códigos internos | adjuntar un archivo y observar su tarjeta |
| Contexto independiente de la subida | Verificado automáticamente | tests `attachment_exposes_durable_document_context_progress_and_chunk_count` y `document_context_failure_does_not_invalidate_upload_and_can_be_retried` | el archivo sigue `Preparado` aunque falle la descarga de Markdown; la tarjeta conserva el error del contexto | adjuntar un documento y observar el segundo estado de su tarjeta |
| Cobertura documental visible | Verificado automáticamente | `AttachmentView.indexedCharacters`, suma derivada de SQLite y test `attachment_exposes_durable_document_context_progress_and_chunk_count` | la tarjeta muestra fragmentos, caracteres consultables y tokens estimados sin afirmar cobertura del texto que el Broker no convirtió | adjuntar un documento y revisar **Contexto preparado** |
| Fragmentos y reintento visibles | Verificado automáticamente | `attachmentContextSummary` y comando `retry_attachment_context` | la tarjeta muestra el tamaño consultable y reintenta solo el contexto, sin duplicar la subida | pulsar **Reintentar contexto** en un adjunto con contexto no disponible |
| Archivos reutilizables por proyecto | Verificado automáticamente | test Rust `project_file_can_be_reused_without_leaking_into_another_project` y test TypeScript `project file library` | un archivo se guarda una vez, aparece en los demás chats del mismo proyecto y nunca cruza a otro proyecto; añadirlo sigue siendo una acción explícita | en un chat con proyecto pulsar **Guardar en proyecto**, abrir otro chat del mismo proyecto y pulsar **Usar en este chat** |
| Instrucciones reutilizables por proyecto | Verificado automáticamente | tests Rust `project_instructions_are_scoped_and_visible_in_the_exact_task_context` y `project_instructions_are_explicit_reusable_context_in_the_broker_prompt` | se aplican en chats normales y semánticos, no dependen de activar la memoria, no cruzan proyectos y la versión usada queda visible | seleccionar un proyecto → **Añadir instrucciones** → enviar un mensaje en uno de sus chats → **Ver contexto utilizado** |
| Contexto de proyecto en flujos | Verificado automáticamente | tests `workflow_publication_freezes_gpt_version_and_creates_durable_node_runs`, `a_published_custom_gpt_profile_reaches_the_broker_request` y prueba de `WorkflowStudio` | cada nodo de modelo recibe las instrucciones y recuerdos activos fijados al publicar; editar o retirar una fuente revoca versiones antiguas y el inspector anticipa el contexto | **Flujos** → asociar un proyecto → seleccionar un nodo GPT o Instrucción rápida → revisar **Contexto del proyecto al publicar** → publicar y ejecutar |
| Vista unificada de conocimiento | Verificado automáticamente | test Rust `project_knowledge_overview_composes_only_the_selected_project_sources` | compone en lectura las instrucciones, archivos y recuerdos del proyecto seleccionado sin duplicarlos ni mostrar fuentes de otro proyecto | seleccionar un proyecto → **Ver conocimiento** y revisar contadores, estados y contenidos |
| Mantenimiento desde la vista de conocimiento | Verificado automáticamente | el mismo test comprueba `remove_project_file` y `set_project_memory_item_enabled` con aislamiento por proyecto | retirar un archivo no lo quita de chats existentes; activar o desactivar un recuerdo conserva su contenido e índice; ambas acciones quedan auditadas | **Ver conocimiento** → **Retirar del proyecto** o **Activar/Desactivar**, cerrar y volver a abrir la vista |
| Navegación desde archivos a sus chats | Verificado automáticamente | `ProjectKnowledgeOverview.fileUsages` y el mismo test Rust comprueban dos conversaciones activas del proyecto | cada archivo muestra sus chats reales y abre el seleccionado mediante el flujo normal; no expone rutas locales ni incluye conversaciones ajenas | **Ver conocimiento** → localizar un archivo → pulsar el título de uno de sus chats |
| Búsqueda y filtros de conocimiento | Verificado automáticamente | tres tests TypeScript de `filterProjectKnowledge` y compilación de producción | busca sin distinguir acentos o mayúsculas, limita a archivos o recuerdos y muestra resultados vacíos sin consultar al Broker ni modificar datos | **Ver conocimiento** → escribir parte de un nombre o recuerdo → alternar **Todo**, **Archivos** y **Recuerdos** |
| Proyección estructurada a Obsidian | Verificado automáticamente | `export_conversation_to_obsidian` y test `obsidian_export_links_project_and_reuses_verified_attachments` | crea notas con YAML, identificadores estables y enlaces a proyecto/adjuntos sin copiar SQLite | abrir una conversación → **Obsidian** → elegir una bóveda |
| Índices de proyecto y memoria | Verificado automáticamente | proyecciones administradas `Indices/Proyectos/<id>.md` y `Memoria/Aprobada.md` | la exportación reúne chats activos y recuerdos aprobados, excluye sensibles y detecta ediciones externas antes de reemplazar | exportar un chat de proyecto, abrir ambos índices y repetir tras editar uno manualmente |
| Adjuntos idempotentes y verificados | Verificado automáticamente | nombre estable por ID, SHA-256 de origen/destino y contador de copias reutilizadas | una segunda exportación no duplica ni vuelve a copiar adjuntos idénticos | exportar dos veces y revisar el aviso verde |
| Conflicto externo protegido | Verificado automáticamente | huella registrada en `export_records` y comprobación independiente de cada adjunto | los cambios hechos en Obsidian sobreviven hasta que el usuario confirma reemplazarlos | editar la nota o un adjunto y volver a exportar |

## Verificación automática

```powershell
cargo fmt --check
cargo test
cargo clippy --all-targets -- -D warnings
pnpm test
pnpm typecheck
pnpm build
```

Resultado: 62 pruebas Rust y 34 pruebas TypeScript correctas, frontend de
producción compilado. Clippy se ejecuta como cierre del corte.

## Siguiente corte

El primer corte vertical de Fase 3 continúa en `PHASE_3_VERIFICATION.md`.
